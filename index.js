const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

// Code your solution here
// function superbowlWin() 
function reducer () { function batteries(){
  let totalBatteries=batteryBatches.reduce((total,m) => total+m);
}
  return totalBatteries=
}